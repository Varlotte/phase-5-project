// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Fave {
  faved_on     DateTime   @default(now())
  unfaved_on   DateTime?
  user         User       @relation(fields: [userId], references: [id])
  userId       Int
  medication   Medication @relation(fields: [medicationId], references: [id])
  medicationId Int

  @@id([userId, medicationId])
}

model User {
  id       Int      @id @default(autoincrement())
  name     String
  email    String   @unique
  password String
  birthday DateTime
  // somehow only show faves affiliated with this user id
  faves    Fave[]
  // currMeds, currConditions, notes TBD
}

model Medication {
  id            Int         @id @default(autoincrement())
  nameBrand     String
  nameGeneric   String
  drugClass     String
  prescribedFor String
  sideEffects   String
  pillImage     String
  // points back to intermediate fave table
  faves         Fave[]
  // implicit many-to-many relationship between medications and treatments
  conditions    Condition[]
}

model Condition {
  id          Int          @id @default(autoincrement())
  name        String
  description String
  medications Medication[]
}
